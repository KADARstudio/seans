// Public configuration only. NEVER put a service-role key or database password here.
// Remains disabled until a dedicated Seans backend and owned site origin are verified.
window.SEANS_ROOM_CONFIG = Object.freeze({ enabled: false, supabaseUrl: '', publishableKey: '', siteOrigin: '' });
